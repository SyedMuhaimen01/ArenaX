<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Solid Background Color -->
    <solid android:color="@color/interactiveColor" />

    <!-- Rounded Corners -->
    <corners
        android:topLeftRadius="@dimen/_16sdp"
        android:topRightRadius="@dimen/_16sdp"
        android:bottomLeftRadius="@dimen/_16sdp"
        android:bottomRightRadius="0dp" />

    <!-- Optional: Add a subtle shadow effect -->
    <stroke
        android:width="@dimen/_1sdp"
        android:color="@color/shadowColor" />
</shape>